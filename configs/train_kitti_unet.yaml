out_dir: runs/kitti_unet
data_root: data/processed
dataset: kitti
batch_size: 16
epochs: 20
num_workers: 4
model_module: src.models.unet
model_class: UNet
model_kwargs:
  in_channels: 4
  out_channels: 1
  base_channels: 32
lr: 0.0001
loss_weights:
  l1: 1.0
  dice: 1.0
  boundary: 0.2
